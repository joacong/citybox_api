require 'spec_helper'

describe CityboxApi do
  it 'has a version number' do
    expect(CityboxApi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
